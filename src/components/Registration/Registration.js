import React from 'react';
import RegistrationForm from './RegistrationForm';

const Registration = () => {
  return (
    <div>
      <RegistrationForm />
    </div>
  );
};

export default Registration;
